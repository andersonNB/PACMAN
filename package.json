{
  "name": "pacman-architecture-practice",
  "version": "0.1.0",
  "private": true,
  "description": "Framework-agnostic Pac-Man-like architecture practice project.",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "demo:console": "npm run build && node dist/demo/console-demo.js",
    "demo:browser": "npm run build && node scripts/serve-demo.mjs"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vitest": "^0.34.6"
  },
  "dependencies": {}
}
